# ===========================================
# OrenaX Backend - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# Server Configuration
PORT=3001
NODE_ENV=production

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters
JWT_EXPIRATION=3600

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# OAuth Redirect URLs
GOOGLE_REDIRECT_URL=https://your-domain.com/auth/callback
FACEBOOK_REDIRECT_URL=https://your-domain.com/auth/callback

# Google Cloud Vertex AI Configuration
GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
# For Railway: Use GOOGLE_CREDENTIALS_JSON instead (base64 encoded)
# GOOGLE_CREDENTIALS_JSON=base64-encoded-service-account-json

# Vertex AI Settings
VERTEX_AI_DEFAULT_MODEL=gemini-2.5-flash
VERTEX_AI_API_ENDPOINT=aiplatform.googleapis.com

# Google Cloud Storage
GCS_BUCKET_NAME=your-gcs-bucket-name
GCS_IMAGE_PATH=images/generated
GCS_ENABLE_PUBLIC_ACCESS=true

# Gemini API v2 Configuration
GEMINI_API_KEY=your-gemini-api-key
GEMINI_DEFAULT_MODEL=gemini-2.0-flash

# ===========================================
# Railway Deployment Notes:
# ===========================================
# 1. Set all these variables in Railway dashboard
# 2. For GOOGLE_APPLICATION_CREDENTIALS on Railway:
#    - Encode your service-account-key.json to base64
#    - Set GOOGLE_CREDENTIALS_JSON with the base64 value
#    - The app will decode it at runtime
# ===========================================
